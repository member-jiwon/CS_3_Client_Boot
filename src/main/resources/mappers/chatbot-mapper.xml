<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="chatbot">

    <select id="answer" resultType="com.kedu.project.chatbot.ChatBotDTO">
        SELECT * FROM chatbot_responses where trigger_text = #{trigger_text}
    </select>

</mapper>